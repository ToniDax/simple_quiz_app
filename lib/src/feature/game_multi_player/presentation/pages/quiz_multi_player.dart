import 'package:flutter/material.dart';

class QuizMultiPlayer extends StatelessWidget {
  const QuizMultiPlayer({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold();
  }
}
