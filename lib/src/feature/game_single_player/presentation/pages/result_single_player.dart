import 'package:flutter/material.dart';

class ResultSinglePlayer extends StatelessWidget {
  const ResultSinglePlayer({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold();
  }
}
