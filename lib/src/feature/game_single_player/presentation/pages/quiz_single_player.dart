import 'package:flutter/material.dart';

class QuizSinglePlayer extends StatelessWidget {
  const QuizSinglePlayer({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      backgroundColor: Colors.white,
      body: Center(
        child: Text("Quiz Single Player"),
      ),
    );
  }
}
